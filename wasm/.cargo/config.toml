# getrandom 0.3 on wasm32-unknown-unknown requires both the `wasm_js` feature
# AND this cfg flag to select the JS backend at compile time.
# See https://docs.rs/getrandom/0.3.3/#webassembly-support
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
